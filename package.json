{
  "name": "codex-subscription-router-patcher",
  "private": true,
  "version": "0.5.0",
  "description": "Build tooling for managed and independent Codex Subscription Router macOS app patches",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tony-ng-vn/codex-subscription-router.git"
  },
  "homepage": "https://github.com/tony-ng-vn/codex-subscription-router#readme",
  "bugs": {
    "url": "https://github.com/tony-ng-vn/codex-subscription-router/issues"
  },
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "npm run check:go && npm run check:js && npm run check:test && npm run check:python && npm run check:shell",
    "check:go": "go test ./... && go vet ./...",
    "check:js": "node --check ui/account-menu.js && node --check ui/thread-subscription.js && node --check ui/ui-test-bridge.cjs",
    "check:test": "node --test test/*.test.cjs",
    "check:python": "python3 -m py_compile scripts/patch_app.py scripts/patch_app_test.py scripts/check_release.py && python3 -m unittest scripts.patch_app_test",
    "check:shell": "bash -n install.sh",
    "release:check": "python3 scripts/check_release.py"
  },
  "devDependencies": {
    "@electron/asar": "4.2.1"
  }
}
